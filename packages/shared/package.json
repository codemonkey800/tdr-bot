{
  "name": "@tdr-bot/shared",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "run-p lint:*",
    "lint:eslint": "eslint --ext .ts src",
    "lint:prettier": "prettier --check src"
  },
  "dependencies": {
    "@tdr-bot/shared": "workspace:*",
    "plaid": "^20.0.0"
  },
  "devDependencies": {
    "@tdr-bot/eslint-config": "workspace:*",
    "esbuild": "^0.20.0"
  }
}
